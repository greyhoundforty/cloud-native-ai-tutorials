apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-app.fullname" . }}
  labels:
    {{- include "llm-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llm-app.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: vault-demo
  namespace: llm-app
spec:
  selector:
    app: vault-demo
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP

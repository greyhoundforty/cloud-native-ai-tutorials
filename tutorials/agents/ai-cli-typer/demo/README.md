# Demo

codeqa: AI-powered code review CLI built with Typer and Claude.

## Prerequisites
- Python >= 3.10, Anthropic API key

## Quick Start
```bash
pip install -e .
export ANTHROPIC_API_KEY=<your-key>
codeqa review path/to/file.py
codeqa ask "What does the auth flow do?"
codeqa chat
```
